#include <stdio.h>
#include <math.h>

int sum(long long n){
    int j,count=0,s=1;
    for (int i=2;i<=sqrt;++i){
        
    return 0;
}

int main(){
    int t;
    long long n;
    scanf("%d",&t);
    for (int i=1;i<=t;++i) {
        scanf("%lld",&n);
        if (sum(n)) printf("YES\n"); else printf("NO\n");
    }
    return 0;
}